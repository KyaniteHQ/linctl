# Support user authorization flow from day one

linctl will support the browser-based OAuth authorization-code flow in the first OAuth implementation, alongside non-interactive OAuth app credentials. This makes the first OAuth cut larger, but it keeps user-approved installs, refreshable tokens, and app installation behavior inside the initial product shape instead of treating them as a later migration.

**Status**: accepted
